# Yield Prediction API

## Description
This API predicts agricultural yield using a trained machine learning model. It supports multivariate inputs for different areas, crops, and years.

## Setup

1. Clone the repository and navigate to the `API` directory.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
